---
it is a: cloud storage
---
### Key Terms:
**bucket**: it's like the folder containing the items
**Object**: Items (like: images, pdf,etc)
**Region**: the place of the bucket
**Permission**: 😐
### Example:
google drive
### Steps:
1. sign in to [AWS Console](https://aws.amazon.com/)
2. Open S3 (only 5gb is free)
3. Configuration:
	1. Bucket name: *yellow-hows-s3-99* (name must be unique)
	2. Object Ownership (default)
	3. Block Public Access settings for this bucket *block all public access* (for safty)
	4. Bucket *disable* (multiple versions of the same file)
	5. Default encryption: (default)
	🚀CREATE BUCKET
___
## security check list 🔒
### set up once ✅
- [ ] **Block public access by default**
	→ S3 > Buckets > Create or select bucket > Permissions > Enable all 4 “Block Public Access” settings
- [ ] **Enable encryption for all objects**
	→ S3 > Bucket > Properties > Default encryption > Turn on → Use AES-256 or AWS KMS
- [ ] **Require TLS for data in transit**
	→ S3 > Bucket > Permissions > Bucket Policy > Add policy that denies s3:* if "aws:SecureTransport" ≠ "true"
- [ ] **Disable public ACLs permanently**
	→ S3 > Account Settings > Check “Ignore Public ACLs” + “Block Public ACLs”
- [ ] **Enable access logging** (optional but smart)
	→ S3 > Bucket > Properties > Server access logging > Turn on → Log to a separate private bucket
### Every Day 🔄
- [ ] **Scan for buckets with public access**
	→ S3 > Buckets > Look for any red “Public” label → Click it → Fix immediately
- [ ]  **Check for unexpected new buckets**
	→ S3 > Buckets list > Look for unfamiliar names → Investigate or delete
- [ ]  **Verify no objects are publicly shared via URL**
	→ Open a few important buckets > Objects > Ensure “Permissions” show “Bucket owner enforced” or private ACLs